// Opt-in proxy route: mounts the dispatcher at /proxy/**. Backends come from
// tlv2proxy.backends (auto-registered by the module).
export { default } from '../../../../src/runtime/server/api/proxy'
