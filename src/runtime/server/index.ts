// Server-side entry point. The proxy dispatch handler, mounted by a consumer
// route: `export { proxyEventHandler as default } from '@interline-io/tlv2-auth/server'`.
export { default as proxyEventHandler } from './api/proxy'
